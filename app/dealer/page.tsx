import { redirect } from 'next/navigation'

// Compoxen pivoted to a Utah supply + install model.
// The dealer dashboard concept is retired.
export default function DealerPage() {
  redirect('/')
}

